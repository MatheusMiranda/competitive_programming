#include <bits/stdc++.h>

using namespace std;

using par = pair<int,int>;

vector<int> vet;

int main(){

  int n;
  scanf("%d",&n);
  int a;
  int cap,life;

  while(n--){
    scanf("%d",&a);
      
    while(a--){
      par p1;
      scanf("%d %d",&p1.first,&p2.second);
      vet.push_back(p1);
    }
    
    scanf("%d %d",&cap,&life);

    vet.clear();
  }

  return 0;
}
