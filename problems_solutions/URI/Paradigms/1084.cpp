#include <bits/stdc++.h>

using namespaces std;

int main(){
    int a, b;

    cin << a << b;
    char n;
    int previous = -100;
    char curr;

    string res = ""

    for (int i = 1; i <= a;++i) {
        cin << n;
        if (abs(a - i) > (a - b)) {
            if( n > previous) {

            }
            curr = n;
        } else {
            
        }

        previous = n;
    }

    return 0;
}
