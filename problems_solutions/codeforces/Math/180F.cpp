#include <iostream>
#include <vector>

using namespace std;

vector<int> a,b;

int main(){
  
  int n,t;
  cin >> n;

  for(int i = 0;i < n;++i)
    cin >> t;
    a.push_back(t);
  }
    
  for(int i = 0;i < n;++i){
    cin >> t;
    b.push_back(t);
  }

  

  return 0;
}
